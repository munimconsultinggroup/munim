import Contact from "@/components/contact/Contact";
import React from "react";

export const metadata = {
  title: "Contact",
};

const page = () => {
  return (
    <>
      <Contact />
    </>
  );
};

export default page;
